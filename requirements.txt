# Offline Neural Facilitator - CORE backend requirements
# ------------------------------------------------------
# These are the only packages required for the backend to boot and serve the
# API. The app is designed to start and degrade gracefully even if the optional
# (heavy) components in requirements-optional.txt are not installed.
#
#   pip install -r requirements.txt
#
# Then optionally add on-device ML capabilities:
#
#   pip install -r requirements-optional.txt

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
httpx>=0.27
numpy>=1.26
requests>=2.31
PyYAML>=6.0
pypdf>=4.2

# Inference clients
openai>=1.40            # OpenAI-compatible client used for Foundry Local + hybrid online
foundry-local-sdk       # Microsoft Foundry Local (offline, on-device inference)
