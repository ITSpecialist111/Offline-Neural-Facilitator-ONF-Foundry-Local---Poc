# Frontend configuration (Vite). Copy to "frontend/.env" to override.
# Base URL of the ONF backend. Defaults to http://localhost:8000.
# The WebSocket URL is derived automatically (http->ws, https->wss).
VITE_API_BASE=http://localhost:8000
